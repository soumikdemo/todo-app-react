import React from "react"

// [const Header = () =>]  same as [function Header ()]
const Header = () => {
    return (
        <header>
            <h1>Todos</h1>
        </header>
    )
}

export default Header